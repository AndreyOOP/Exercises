package ItermidTest1.VirusAdvertising;

public class VirusAdvertising {

    public static void main(String[] args) {

        //choose next node & mark the node as visited
        // is next node
            //go to next node & mark as visited, if no avail nodes - go to previous node
    }

 /*   static void dfs(Graph g, Node previous, Node currrent){

        current.markVisited();

        Node next = findAvailable();

        if(next != null)
            dfs(g, next);
        else
            dfs(g, previous);
    }*/

}
